{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "IndividualTypeDescription.jsonschema",
  "title": "TypeVersionHash",
  "description": "IndividualTypeDescription",
  "type": "object",
  "properties": {
    "type_name": {"type": "string", "maxLength": 255},
    "fields": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Field"
      }
    }
  },
  "required": ["type_name", "fields"],
  "$defs": {
    "Field": {
      "type": "object",
      "properties": {
        "name": {"type": "string"},
        "type": {"$ref": "#/$defs/FieldType"}
      },
      "required": ["name", "type"]
    },
    "FieldType": {
      "type": "object",
      "properties": {
        "type_id": {"type": "integer", "minimum": 0, "maximum": 255},
        "length": {"type": "integer", "minimum": 0},
        "string_length": {"type": "integer", "minimum": 0},
        "nested_type_name": {"type": "string", "maxLength": 255}
      },
      "required": ["type_id", "length", "string_length", "nested_type_name"]
    }
  }
}
